name: Java CI with Maven

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: joaofcmont/academy/.github/actions/maven_build@main

      - name: Build Docker image
        uses: joaofcmont/academy/.github/actions/docker_build@main
        with:
          path_dockerfile: "src/main/docker/Dockerfile.jvm"  
          image_name: "myapp"
          tag_name: ""  
      
      - name: Login and Push the image to Docker Hub
        uses: joaofcmont/academy/.github/actions/dockerHub@main
        with:
          path_dockerfile: "src/main/docker/Dockerfile.jvm"
          secrets: 
            dockerhub_username: ${{ joaofranciscomonteiro175 }} 
            dockerhub_token: ${{ ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAII6+jkB7lHBzcdB29auXUL0kPo2s8HUDu/G5ijHDiOkH jfcmo98647@gmail.com }}   





